import React from 'react'

const NotFoundPage = () => (
  <section className="section">
    <div className="section__inner">
      <h1 className="section__title">OOOOPS, PAGE NOT FOUND</h1>
      <h3 className="section__subtitle">
        The page you are trying to access was not found, so it cannot be
        displayed.
      </h3>
    </div>
  </section>
)

export default NotFoundPage
